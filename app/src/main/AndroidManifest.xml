<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="orihuel.vilaplana.angel.android_layouts">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".FormActivity"
                android:label="Contacts"
                android:theme="@style/FormTheme">
        </activity>
        <activity android:name=".CalculatorActivity">
        </activity>
        <activity android:name=".ImagesActivity">
        </activity>
        <activity android:name=".MovieDownloadActivity" android:label="Descàrrega de pel·lícules">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>